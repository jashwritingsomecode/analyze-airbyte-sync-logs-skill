# Deployment files

Start with [the deployment guide](../../DEPLOYMENT_GUIDE.md).
Run all deployment commands from the extracted package root, which contains
`kustomization.yaml`. The CronJob ships with `suspend: true` for manual verification.
The JSON schema is in [the event reference](../../docs/EVENT_REFERENCE.md).
